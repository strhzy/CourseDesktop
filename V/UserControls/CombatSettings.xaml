<Page x:Class="DnDPartyManager.V.UserControls.CombatSettings"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:wpfui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:local="clr-namespace:DnDPartyManager.V.UserControls"
      mc:Ignorable="d"
      Title="CombatSettings">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Название боя -->
        <wpfui:TextBox Grid.Row="0" 
                       Text="{Binding SelectedItem.Name, UpdateSourceTrigger=PropertyChanged}" 
                       Margin="0,0,0,10" 
                       PlaceholderText="Название боя" />

        <!-- Игроки -->
        <GroupBox Grid.Row="1" Header="Игроки" Margin="0,0,0,10">
            <ListBox ItemsSource="{Binding CombatPlayers}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Name}" Width="150" Margin="0,0,10,0" />
                            <wpfui:TextBox Text="{Binding CurrentHitPoints, UpdateSourceTrigger=PropertyChanged, StringFormat={}{0:D}}"  
                                           Width="50" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>

        <!-- NPC -->
        <GroupBox Grid.Row="2" Header="NPC" Margin="0,0,0,10">
            <ListBox ItemsSource="{Binding CombatNpcs}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Name}" Width="150" Margin="0,0,10,0" />
                            <wpfui:TextBox Text="{Binding CurrentHitPoints, UpdateSourceTrigger=PropertyChanged, StringFormat={}{0:D}}" 
                                           Width="50" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>

        <!-- Враги -->
        <GroupBox Grid.Row="3" Header="Враги">
            <ListBox ItemsSource="{Binding CombatEnemies}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Name}" Width="150" Margin="0,0,10,0" />
                            <wpfui:TextBox Text="{Binding Hit_points, UpdateSourceTrigger=PropertyChanged, StringFormat={}{0:D}}" 
                                           Width="50" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>
    </Grid>
</Page>