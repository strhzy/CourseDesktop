<Page x:Class="DnDPartyManager.V.UserControls.StorySettings"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:DnDPartyManager.V.UserControls"
      mc:Ignorable="d"
      Title="StorySettings">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="20*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" Text="{Binding SelectedStory.Name}" HorizontalAlignment="Center" VerticalAlignment="Center" Width="Auto"></TextBox>
        <TextBox Grid.Row="1" Text="{Binding SelectedStory.Description}"></TextBox>
    </Grid>
</Page>
